{
  "name": "radar-care-fleet-demo",
  "version": "0.1.0",
  "private": true,
  "description": "Reproducible tooling for the hosted Radar Care Fleet Demo dashboard.",
  "scripts": {
    "cloudflare:stage": "bash scripts/stage_cloudflare_dashboard.sh",
    "cloudflare:deploy": "bash scripts/deploy_cloudflare_dashboard.sh",
    "cloudflare:check": "npm run cloudflare:stage && bash scripts/wrangler_cloudflare.sh deploy --dry-run",
    "cloudflare:login": "bash scripts/wrangler_cloudflare.sh login",
    "cloudflare:whoami": "bash scripts/wrangler_cloudflare.sh whoami"
  },
  "devDependencies": {
    "wrangler": "4.106.0"
  }
}
